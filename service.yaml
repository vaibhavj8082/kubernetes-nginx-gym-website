apiVersion: v1
kind: Service
metadata:
  labels:
    app: gym
  name: gym
spec:
  ports:
  - port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: gym
  type: NodePort
status:
  loadBalancer: {}
